/**
 * @deprecated useUpdateEffect has been removed.
 * Keep this stub so existing imports fail loudly during refactors.
 */
export function useUpdateEffect(): never {
  throw new Error(
    'useUpdateEffect has been removed. Inline useEffect instead.'
  );
}

export default useUpdateEffect;
